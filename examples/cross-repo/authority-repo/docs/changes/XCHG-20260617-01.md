# XCHG-20260617-01 — 訂單狀態新增 cancelled(跨 repo)

- 權威:authority-repo / docs/contracts/api-contract.md
- 涉及 repo:repoA(前端)、repoB(後端)
- 風險分級:中(行為擴充、非破壞性新增列舉值)
- 契約版本:v2 → v3(status 列舉新增 `cancelled`)
- 各 repo 子變更:repoA → CHG-20260617-07;repoB → CHG-20260617-11
- 整合驗收:ACC-INT-20260617-01(端到端:後端可取消、前端正確顯示 cancelled)
- 狀態:整合驗收通過

## 動機 / 整體意圖
使用者需可取消未出貨訂單。需在契約加入 `cancelled` 狀態,前後端一致支援。

## 各 repo 影響與順序
1. 權威先把 api-contract.md 的 status 列舉加 `cancelled`,VERSION → v3。
2. repoB(後端,契約提供者)實作取消端點與狀態轉移,指標更新到 v3。
3. repoA(前端,消費者)顯示/操作 cancelled,指標更新到 v3。
4. 整合驗收:端到端取消流程通過,XCHG 收尾。

> 相容性:新增列舉值對舊客戶端為向後相容;舊前端忽略未知狀態不致崩潰,但無法操作取消,故仍需 repoA 跟進。
