---
name: agent-hierarchy
description: >
  代理編制與階層:多個 agent 如何編制成階層分工——每個 agent 有編號 + 固定角色與任務範圍,不得越權;
  允許子 agent 在自己範圍內再建子 agent(遞迴),但權限只能縮不能放、且由上層主動管理;典型鏈為
  分析 → 實作(主實作 agent 分派子 agent 並自檢)→ 獨立驗收。當一個任務要由多個 agent 分工、或某 agent
  需要再分派子 agent 時讀本檔。
---

# agent-hierarchy — 代理編制與階層

> 語言 / Language: **繁體中文** · [English](agent-hierarchy.md)

## 用途

把「多個 agent 一起做事」變成**有編制、有界線、可管理**的階層,而不是一堆 agent 各自亂跑。核心:**每個 agent 都被編號、被授予固定任務與範圍,不能做份外的事**;需要更多人力時可往下建子 agent,但**由上層主動管理、權限只能縮不能放**。

## 何時讀

- 一個任務要由多個 agent 分工(如 分析 / 實作 / 驗收)
- 某個 agent 工作量大,需要再分派子 agent 處理

## 編制原則

1. **編號 + 固定角色 + 固定範圍**:每個 agent 有唯一編號、固定角色、固定任務範圍(scope)與讀寫權限。建議命名反映階層:`A1`(分析)、`I1`(實作主)、`I1.1`/`I1.2`(I1 的實作子)、`V1`(驗收)。
2. **不得越權**:agent 只能做份內任務、只能讀寫被授予的範圍。**需要做範圍外的事 → 回報上層**,由上層決定(改派他人 / 擴大授權),**不可自行擴張**。
3. **遞迴授權,權限只能縮不能放**:子 agent 可在「自己被授予的範圍內」再建子 agent(孫),但:
   - 給下層的範圍**只能是自己範圍的子集**(權限不可放大);
   - 整條鏈(編號、角色、範圍、父子關係)都要登記在協調文件;
   - **由上層主動管理**:上層負責指派、追蹤、收斂,不能放生。
4. **上層職責**:指派任務 → 追蹤進度 → 確認與驗證子代理產出 → 收斂彙整 → 把錯誤入知識庫(見 agent-worklog)。

> 適用範圍:**單人或團隊皆適用**。個人用一個會開子代理的 AI,同樣可用此編制(分析/實作/驗收都由 AI 子代理擔任),你就是最上層管理者。

## 遞迴建置:現況與建議

- **巢狀子代理已支援**:子 agent 可再建子 agent;**最大深度 5**。所以遞迴授權路徑可用,不必退扁平。
- **但建議保持淺(2–3 層)**:層數越深,協調、追蹤、回溯成本越高、上層越難管理。需要更深時,通常代表任務該再拆分或重新設計,而非一路往下疊。
- **用 tools allowlist 控制「誰能 spawn」**:是否能再開子代理,由該 agent 的**工具授權**決定——只有需要分派的角色(如實作主 I1)才授予 `Agent`/spawn 工具;不需分派的角色(驗收 V1、純實作子 I1.x)**不給 `Agent` 工具**,從機制上擋住它再開子代理。這把「權限只縮不放」變成可由工具清單強制,而非只靠自律。
- **不變的治理原則**:不論層深,編號、固定範圍、不得越權、登記編制、上層主動管理、實作者不自驗都不變。

實務:超過 2–3 層前先想「是不是該把任務拆細或重畫邊界」;接近深度 5 應視為警訊。

## 典型鏈(實作情境範例)

```
A1 分析 ──► I1 實作主 ──► V1 獨立驗收
            ├─ I1.1 實作子(模組X)
            └─ I1.2 實作子(模組Y)
```

1. **分析 agent(A1)**:依需求產出 Guideline / 結構 / 影響分析——**只分析、不實作**。
2. **實作主 agent(I1)**:依 A1 的分析結果,把實作拆成子任務,分派給**實作子 agent(I1.1, I1.2…)**;每個子 agent 只做被分配的模組、只寫該模組範圍。
3. **I1 確認與驗證**每個子 agent 的產出(整合、跑測試、查一致性);全部子任務完成後彙整。
4. **全部實作完畢後**,交由**獨立驗收 agent(V1)**——V1 ≠ 實作鏈任何 agent、且**唯讀**——做多情境驗收,產出 ACC(見 independent-acceptance)。
5. 鐵律:**I1 不可自驗**(球員不可兼裁判);**V1 不可改碼**(發現問題→回 modification-guide 交實作鏈修)。

## 角色職責目錄(role catalog)

每個角色的「職責內容」——做什麼、吃什麼、產出什麼、不可做什麼、交給誰、對應哪個 ai-sdlc 階段。工具授權見下一節。

### 上層 / orchestrator(parent)
- **職責**:拆解與指派、追蹤進度、確認與驗證子代理產出、收斂彙整、把錯誤入知識庫;在每個關卡查停點契約決定要不要回報人類。
- **輸入**:使用者需求 / 上層交付的任務。**輸出**:編制(coordination)、彙整結果、對人的回報。
- **不可**:把該停的關卡自行放行(見 autonomy);放生子代理不管。
- **階段**:總控(跨全程)。**交給**:A1 → I1 → V1。

### A1 分析(analyst)
- **職責**:把需求轉成可執行的依據——AI Guideline、四種結構、影響分析 / 模組切分建議。
- **輸入**:需求(口語/文件)。**輸出**:`docs/ai-guideline.md`、`docs/structure/*`、影響分析。
- **可做**:釐清、設計、定介面契約與驗收條件。**不可**:寫實作程式碼。
- **階段**:需求分析 + 結構設計。**交給**:I1(附模組切分與整合點)。

### I1 實作主(lead implementer)
- **職責**:依 A1 的分析把實作拆成子任務、分派子代理、建共用基礎、整合、做實作層自檢、留變更記錄。
- **輸入**:A1 產出。**輸出**:`src/*`、`docs/changes/CHG-*`(含風險、實作者)。
- **可做**:寫自己範圍的程式、分派 I1.x、整合與跑測試自檢。**不可**:自宣告驗收通過(不出 ACC)、放寬高風險停點、改 A1 的結構文件語意(要改走 modification-guide)。
- **階段**:修改治理 + 實作。**交給**:V1(附驗收條件來源)。

### I1.x 實作子(sub-implementer)
- **職責**:只完成被分配的單一模組。
- **輸入**:I1 指派的子任務 + 鎖定範圍。**輸出**:該模組程式 + 自測。
- **可做**:寫自己模組、import 共用層。**不可**:越出模組、改別人模組、再開子代理(無 `Agent` 工具)。
- **階段**:實作。**交給**:回報 I1。

### V1 驗收(verifier)
- **職責**:獨立、多情境驗收成果,產出 ACC;對齊來源文件不採實作者自述;盡量跨模型。
- **輸入**:驗收條件來源(Guideline §7 / 該 CHG)+ 成果(程式/系統)。**輸出**:`docs/acceptance/ACC-*`。
- **可做**:讀碼與條件、跑測試/腳本/CLI/GUI 驗證、寫自己的 ACC。**不可**:改被驗的碼或結構、開子代理。
- **階段**:驗收。**交給**:通過→收尾回填 CHG;未過→回 `modification-guide` 交 I1 修。

### 整合者 integrator(並行時)
- **職責**:多個並行變更動到同一結構 / 彼此相依時,做一次整合驗收確認彼此相容。
- **輸入**:各 claim 的產出 + 各自 ACC。**輸出**:整合 ACC。**不可**:略過(各自綠燈不等於整體相容)。

### 審閱者 reviewer(選用)
- **職責**:對文件/決策做獨立審閱(doc-integrity 中無法程式化的語意一致性、決策是否仍成立)。
- **可做**:唯讀審閱、提出問題記入文件。**不可**:直接改被審的內容(交回對應角色)。

> 一人/一 agent 可身兼多角色,但**實作與驗收必須分離**(I1 鏈 ≠ V1);其餘角色可視規模合併。

### 角色 → 載入 references(依職責載入子集)

啟動某角色時,只載該角色需要的 references 子集(基本集),再依偵測情境追加:

| 角色 | 基本載入(references) | 偵測到時追加 |
|------|----------------------|--------------|
| orchestrator | agent-hierarchy · agent-worklog · autonomy · doc-integrity | 視情況全部 |
| analyst (A1) | requirement-analysis · structure-design · doc-integrity | multi-repo→cross-repo |
| lead-implementer (I1) | modification-guide · structure-design · agent-hierarchy · agent-worklog · doc-integrity | multi-repo→cross-repo;有 CI→ci-cd |
| sub-implementer (I1.x) | modification-guide · agent-worklog | — |
| verifier (V1) | acceptance-verification · independent-acceptance · doc-integrity | — |
| integrator | independent-acceptance · cross-agent · doc-integrity | — |
| reviewer | doc-integrity · independent-acceptance | — |

情境追加(偵測旗標):multi-repo→`cross-repo`、並行/交接→`cross-agent`、自主連跑→`autonomy`、有 CI/CD→`ci-cd`。

> **程式可讀**:本表的機器版在 [`assets/role_refs.json`](../assets/role_refs.json)(**JSON 為程式的單一真相,本表是其人類視圖,兩者須一致**)。外部協調器可用 [`scripts/role_loadout.py`](../scripts/role_loadout.py) 查:`python3 scripts/role_loadout.py --role verifier`、`--role I1 --multi-repo --cicd`(印出該載清單;`--json` 給程式取用)。

## 角色啟動規格(工具授權 allowlist)

每個 agent **啟動時依角色給定 tools allowlist**(最小權限)。這份規格是「當外部工具(python 等)呼叫/執行一個 AI 來擔任某角色」時的授權依據;**也適用於傳統的純 CLI / GUI 操作**——「可執行」涵蓋跑指令、跑測試、操作 GUI,不是只給 AI 用。

| 角色 | 可 spawn(`Agent` 工具) | 可寫程式/結構 | 可執行(CLI / 腳本 / GUI) | 說明 |
|------|------------------------|---------------|---------------------------|------|
| 分析 A1 | ✗ | ✗(只寫 docs/) | ✓(讀碼、跑分析工具) | 只分析,不實作 |
| 實作主 I1 | ✓(分派 I1.x) | ✓ 自己範圍 | ✓ | 需分派才給 `Agent` |
| 實作子 I1.x | ✗ | ✓ 自己模組 | ✓ | 不再分派 → 不給 `Agent` |
| **驗收 V1** | **✗(tools 不含 `Agent`)** | **✗(對被驗的碼/結構唯讀)** | **✓ 可跑測試/腳本/CLI/GUI 來驗證** | 只讀碼與條件、只寫自己的 ACC;**不可開子代理、不可改被驗的碼**,但**可執行**以進行多情境驗證 |

> **「唯讀」≠「不能執行」**:V1 的唯讀是指「不可修改被驗的程式與結構文件、不可 spawn 子代理」;它**仍可**用 python/CLI 跑測試、用 GUI 操作來驗證(這是驗收必需)。把「不含 `Agent` 工具」寫進 V1 的啟動規格,等於用工具清單在機制上保證它不會「邊驗邊改」或再開代理——比只靠自律可靠。

## 編制登記(協調文件)

在 `docs/coordination.md` 維護編制表,讓任何人/agent 一眼看懂誰在做什麼、誰管誰、給了什麼工具:

```markdown
# 代理編制
| 編號 | 角色 | 上層 | 任務 / 範圍 | 讀寫權限 | 工具授權 | 狀態 |
|------|------|------|-------------|----------|----------|------|
| A1   | 分析 | —    | 需求/結構/影響分析 | 讀碼;只寫 docs/ | 無 Agent;可執行分析 | 完成 |
| I1   | 實作主 | —  | 協調實作、整合 | RW src/ | 有 Agent(可分派);可執行 | 進行中 |
| I1.1 | 實作子 | I1 | 模組 X | RW src/x | 無 Agent;可執行 | 進行中 |
| I1.2 | 實作子 | I1 | 模組 Y | RW src/y | 無 Agent;可執行 | 完成 |
| V1   | 驗收 | —    | 多情境驗收 | 對碼唯讀;只寫 docs/acceptance | **無 Agent;可執行(跑測試/CLI/GUI)** | 待命 |
```

## 與既有流程的關係

- 延伸 `cross-agent` 的角色/權限/claim:本檔再加「**編號 + 階層 + 遞迴授權(只縮不放)+ 上層主動管理**」。
- 延伸 `agent-worklog`:每個編號 agent 進場先寫 worklog;遇錯回報上層;上層彙整錯誤入知識庫。
- 延伸 `independent-acceptance`:實作鏈(I1 及其子)做完 → 交獨立的 V1 驗收,實作者不自驗。
- 對應 ai-sdlc 階段:A1≈需求分析/結構設計,I1 鏈≈修改治理+實作,V1≈驗收。
