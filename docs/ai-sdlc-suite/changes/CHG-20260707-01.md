# CHG-20260707-01 — 建立 ai-sdlc-suite 大補帖(plugin + MCP + hooks + CI,皆選配)

- Project: ai-skills / ai-sdlc-suite
- Branch: claude/quirky-margulis-cad156
- Date: 2026-07-07 (UTC+0)
- Type: 新功能(整合發佈層;不動兩個 skill 本體)
- Proposed by: 使用者(「MCP + hook 為選配 如果能像superpower那樣一整包的大補帖也行」)
- Implemented by: Claude(Cowork session, worktree quirky-margulis-cad156)
- Risk: 中(新增 hooks 可攔截工具呼叫+MCP server;緩解:預設 warn、全部選配、核心零改動)
- Commit/PR: <收尾時回填>
- 重複性檢查(Recurrence check): 無重複(首筆「發佈/接線層」動機;先前 CHG 皆為契約層。與 ai-sdlc 庫 KN-001 呼應:本 CHG 正是把「lint 存在但沒接線」補上執行面)
- Skill: ai-sdlc v1.17
- Related: docs/ai-sdlc-suite/ai-guideline.md v1.0;ai-sdlc 審視結論(自律→自動的接線缺口);autopilot CHG-20260706-01(runner exit code 契約)

## 動機
見 Guideline 第 1 節:三層拼圖缺「送達與強制」——CI(普適強制)、MCP(跨平台供給)、hook(Claude Code 即時強制);以 Superpowers 式 plugin 一鍵打包,全部選配。

## 決策與取捨
| 決策 | 選項 | 前提假設 | 為何這樣選 |
|------|------|----------|------------|
| plugin 內 skills 為建置複本(build_suite 同步) | 單一位置/symlink | plugin 需自包含;dist 已有「建置產物入 repo」慣例 | 單一真相仍在 skills/,同步腳本冪等可重跑 |
| hooks 預設 warn,block 需 env 顯式開 | 預設 block | 誤攔的成本>漏警的成本(使用者明定「選配」) | warn 先建立信任,block 留給要硬治理的專案 |
| MCP 工具全部唯讀 | 允許寫(建 CHG 等) | 寫入屬 agent 職責,MCP 只供能力 | 供給層不碰帳本,避免第二寫入路徑 |
| 本 repo 同輪接 governance workflow | 只出範本 | dogfood:審視發現「lint 沒接線」是實證缺口 | 範本+自用一次到位,CI 從此看門 |

## 修改指引

### 目標
Guideline FR-1〜FR-5 落地,AC-1〜AC-5 全過;核心 skill 檔案零改動。

### Global Constraints(全域約束——每個 task 都要遵守)
- 全 stdlib、無網路呼叫;hooks 內部錯誤一律 exit 0(絕不阻斷正常工作)
- 不修改 skills/ 下任何既有檔案;plugin 內複本只由 build_suite.py 產生
- manifests 與所有 JSON 必過 json.tool;時間 UTC+0;無 secrets

### Tasks(checkbox=續作點)
- [x] T1. plugin 骨架:root .claude-plugin/marketplace.json + plugins/ai-sdlc-suite/.claude-plugin/plugin.json + plugin README.md
  - interfaces: consumes Guideline FR-1 / produces 三檔
  - test: 兩 JSON valid;README 含安裝兩步驟
- [x] T2. MCP server:plugins/ai-sdlc-suite/mcp/ai_sdlc_mcp.py + .mcp.json(五工具)
  - interfaces: consumes 既有四腳本 CLI+knowledge 檔式 / produces server 單檔+設定
  - test: echo 管線 smoke:initialize/tools list=5/三個 tools call 回有效 JSON
- [x] T3. hooks:hooks/hooks.json + session_start.py + pre_edit_gate.py + stop_closeout.py
  - interfaces: consumes Claude Code hook stdin JSON / produces 四檔
  - test: 合成 stdin 三支 exit 0;block/warn 模式行為相符;stop_hook_active 防迴圈
- [x] T4. CI:plugins/ai-sdlc-suite/ci-templates/{pre-commit-config.yaml,governance.yml} + 本 repo .github/workflows/governance.yml
  - interfaces: consumes 既有 lint 腳本 / produces 範本×2+自用 workflow
  - test: workflow 內每個檢查指令本地跑通(等效驗證)
- [x] T5. build_suite.py 同步 skills 複本入 plugin 並執行
  - interfaces: consumes skills/ 兩目錄 / produces plugin/skills 複本
  - test: 同步後對複本跑 bilingual_check 通過;重跑第二次輸出 0 變更(冪等)
- [x] T6. 驗證 AC-1〜AC-5(含 git diff 確認核心零改動)
  - interfaces: consumes T1-T5 / produces 驗證輸出(入 ACC)
  - test: 全部斷言通過
- [x] T7. README 大補帖章節+目錄樹
  - interfaces: consumes T1-T5 / produces README 更新
  - test: README 含 marketplace add / plugin install 指令
- [x] T8. ACC-20260707-01 收尾;Commit/PR 回填;commit+PR+merge
  - interfaces: consumes AC 證據 / produces ACC+PR
  - test: lint 必填欄齊;PR merged

### 結構文件更新
- [ ] suite 為發佈層,結構記於本 CHG 影響範圍+README 樹(Guideline §6 已載;無獨立 structure/ 需求)

### 風險與回滾
- 風險:hook 誤攔(緩解:warn 預設+off 開關+錯誤即放行);plugin 複本漂移(緩解:build_suite 冪等,CI 可加同步檢查);MCP 協定相容(緩解:僅用 initialize/tools 最小面)
- 回滾:revert 本 commit;plugin 目錄整體移除即回原狀,核心 skill 不受影響

## 代決事項(confirm gate 記錄)
使用者已定方向(選配+大補帖)。代決:plugin 命名 ai-sdlc-suite;hook 模式 env 名 `AI_SDLC_HOOK_MODE`(warn/block/off);MCP server 名 `ai-sdlc`;本 repo workflow 檔名 governance.yml。

## 審議判決(中風險;spawn 停滯→序列自審,註明限制)

```
[verdict] risk | claude-fable-5 | pass | hooks 三模式設計+錯誤即放行 | 最壞情況=多一行警示;block 是顯式選擇
[verdict] impact | claude-fable-5 | pass | git diff 斷言核心零改動 | 全部增量;移除 plugin 目錄即完全回退
[verdict] drift | claude-fable-5 | concern→resolved | plugin 內 skills 複本=第二份表示 | 定位為建置產物(同 dist),由 build_suite 單向生成,CI 可驗同步
```

## 狀態
已驗收(見 ../acceptance/ACC-20260707-01.md)
